package bst;

public class BST23Tree {
	Node root;
	
	//Node-contains link atmost to 3 other node
	private class Node{
		private Key k1,k2;
		private Value v1,v2;
		private Node left,middle,right;
		public Node(Key k, Value v){
			if(k1==null){k1=k; v1=v}
			else if(k2==null){k2=k; v2=v}
		}
	}
	
	//Inserts a key
	public void put(Key k, Value v){
		if (root == null) {
			Node n=new 
		}
	}
}
